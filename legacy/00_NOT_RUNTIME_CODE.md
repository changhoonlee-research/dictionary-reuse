# Legacy research archive

`legacy/` contains only inactive research ideas explored before the final DiR release.

The released experiment is implemented entirely by `run.py` and `dictionary_reuse/`. Nothing in this directory is imported, executed, or required for reproduction.

Only the conceptual variants are retained here. Historical implementation copies and disabled configuration details are intentionally omitted so the release stays small and readable.
